name: Format Widget Tests Once

on:
  push:
    branches:
      - feat/kedis-categories
    paths:
      - test/task_interaction_regression_test.dart
      - test/widget_test.dart
      - .github/workflows/format-widget-tests-once.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          ref: feat/kedis-categories

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Apply Dart formatter
        run: dart format test/task_interaction_regression_test.dart test/widget_test.dart

      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            echo "No formatter changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add test/task_interaction_regression_test.dart test/widget_test.dart
          git commit -m "style(tests): apply canonical widget test formatting"
          git push origin HEAD:feat/kedis-categories
